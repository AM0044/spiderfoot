# Runtime dependencies. Pinned exactly (==) so vulnerability scanners
# (OpenSSF Scorecard / OSV-Scanner) can match advisories against the real
# installed versions instead of flagging the package's full history.
# Dependabot keeps these pins current (grouped minor/patch, weekly).
adblockparser==0.7
dnspython==2.8.0
ExifRead==3.5.1
CherryPy==18.10.0
cherrypy-cors==1.7.0
Mako==1.3.12
beautifulsoup4==4.15.0
lxml==6.1.1
netaddr==1.3.0
pysocks==1.7.1
requests==2.34.2
ipwhois==1.3.0
phonenumbers==9.0.34
pypdf==6.14.2
python-whois==0.9.6
secure==2.0.1
pyOpenSSL==26.3.0
python-docx==1.2.0
python-pptx==1.0.2
networkx==3.6.1
cryptography==49.0.0
publicsuffixlist==1.0.2.20260722
openpyxl==3.1.5
pyyaml==6.0.3
